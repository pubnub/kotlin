package com.pubnub.api.subscribe.eventengine

import org.junit.jupiter.api.Test

class SubscribeEventEngineTest {

    @Test
    internal fun `should pass invocations for handling when event provided`() {
        // toDo it is hard to stub transition function because it is package level function
        // something like static function but not bind to class(and of course not bind to any instance of a class)
        // maybe it would be better to create class "TransitionMaker" and move transition function there?
    }
}
